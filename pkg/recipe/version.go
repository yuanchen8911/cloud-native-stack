package recipe

const (
	// APIDomain is the API domain for recipe resources
	APIDomain = "cns.nvidia.com"

	// APIVersion is the current API version for recipes
	APIVersion = "v1alpha1"

	// FullAPIVersion is the complete API version string
	FullAPIVersion = APIDomain + "/" + APIVersion

	// Kind is the resource kind for recipes
	Kind = "Recipe"
)
